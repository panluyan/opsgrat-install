# -*- coding: utf-8 -*-
  
import os, logging

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))

########################## db ##########################
DB_NAME = "{{ mysql_sso_db }}"
DB_USER = "{{ mysql_sso_user }}"
DB_PASSWORD = "{{ mysql_sso_user_password }}"
DB_HOST = "{{ mysql_sso_host }}"
DB_PORT = "{{ mysql_sso_port }}"

########################## db ##########################

REDIS = {
    "HOST": "{{ redis_host }}",
    "PORT": {{ redis_port }},
    "PASSWORD": '{{ redis_passwd }}'
}

RABBIT_MQ = {
    "HOST": "{{ rabbitmq_host }}",
    "PORT": "{{ rabbitmq_port }}",
    "USER": "{{ rabbitmq_user }}",
    "PASSWORD": '{{ rabbitmq_passwd }}',
}

# 系统名称和子系统名称对应
SYSTEM_NAME = 'SSO'

# 日志目录
LOG = '{{ log_dir }}'
logger = logging.getLogger('sso')

DEBUG = False
